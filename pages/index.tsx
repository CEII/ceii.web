import type { NextPage } from 'next';
import Footer from '@components/Footer'

const Home: NextPage = () => {
  return(
    <div className='bg-primary'>     
      <Footer/>
    </div>
  )
};

export default Home;
